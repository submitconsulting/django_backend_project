









# create user objecto to attach to our Producer object

#def create_producer_user_callback(sender, instance, **wwargs):
#	producer, new = Producer.objects.get_or_create(user=instance)
#post_save.connect(create_producer_user_callback, User)
